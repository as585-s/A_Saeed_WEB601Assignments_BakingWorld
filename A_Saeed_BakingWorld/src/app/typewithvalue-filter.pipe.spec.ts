import { TypewithvalueFilterPipe } from './typewithvalue-filter.pipe';

describe('TypewithvalueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypewithvalueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
